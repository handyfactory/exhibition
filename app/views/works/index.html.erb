<h1>3D printer 작품 전시회</h1>
<!-- 새로운 작품을 업로드 할 수 있는 페이지-->

<%= link_to '새 작품 올리기', new_work_path %>
<!--현재 저장된 새로운 작품들을 보여준다-->
<%= link_to '작가페이지', makers_path %>



<table>
    <tr>
        <th>제목</th>
        <th>설명</th>
    </tr>
    <%@works.each do |w|%>
    <tr>
        <td><%=w.title%></td>
        <td><%=w.desc%></td>
        <td><%=link_to '상세보기', "/works/#{w.id}"%></td>
        <td><%=link_to '수정', "/works/#{w.id}/edit"%></td>
        <td><%=link_to '지우기', "/works/#{w.id}", method: "delete", data: {confirm: "ㄹㅇ?"}%></td>
    </tr>
<%end%>
</table>

